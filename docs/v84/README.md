<!-- v111: autogenerated by tools/docs/generate_kernel_pages.py -->
# v84 — σ-ZKProof

Canonical directory: [`src/v84/`](../../src/v84/).

## Abstract

v84 kernel — see source header for the authoritative description.

## Source

- `src/v84/creation_os_v84.c`
- `src/v84/zkproof.c`
- `src/v84/zkproof.h`

## Self-test

```bash
make check-v84
```

## Relation to the σ-stack

Layer membership (cf. `docs/AGI_ARCHITECTURE.md`):

- Layer 3–4 (σ-Governance + Reasoning) — σ-Cortex primitives, benchmarks, schemas, and fabric/intellect wiring.

## Source-header excerpt

```c
/* SPDX-License-Identifier: AGPL-3.0-or-later
 *
 * Creation OS v84 — σ-ZKProof self-test driver.
 */

#include "zkproof.h"
#include "../v81/lattice.h"

#include <inttypes.h>
#include <stdint.h>
#include <stdio.h>
#include <string.h>

static uint64_t g_pass = 0;
static uint64_t g_fail = 0;

#define CHECK(cond) do {                                 \
    if ((cond)) { ++g_pass; }                            \
    else {                                               \
        if (g_fail < 16) {                               \
            fprintf(stderr, "FAIL %s:%d: %s\n",          \
                    __FILE__, __LINE__, #cond);          \
        }                                                \
        ++g_fail;                                        \
    }                                                    \
} while (0)

static uint64_t xs(uint64_t *s)
{
    uint64_t x = *s;
    x ^= x << 13; x ^= x >> 7; x ^= x << 17;
    *s = x;
    return x;
}

```
