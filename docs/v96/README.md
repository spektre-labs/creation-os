<!-- v111: autogenerated by tools/docs/generate_kernel_pages.py -->
# v96 — σ-Diffusion

Canonical directory: [`src/v96/`](../../src/v96/).

## Abstract

v96 kernel — see source header for the authoritative description.

## Source

- `src/v96/creation_os_v96.c`
- `src/v96/diffusion.c`
- `src/v96/diffusion.h`

## Self-test

```bash
make check-v96
```

## Relation to the σ-stack

Layer membership (cf. `docs/AGI_ARCHITECTURE.md`):

- Layer 4 (Reasoning) — σ-Attractor: stability, convergence, phase-portrait visualisations.

## Source-header excerpt

```c
/* SPDX-License-Identifier: AGPL-3.0-or-later
 *
 * Creation OS v96 — σ-Diffusion self-test driver.
 */

#include "diffusion.h"

#include <stdint.h>
#include <stdio.h>
#include <string.h>

static uint32_t pass_count = 0;
static uint32_t fail_count = 0;

#define CHECK(cond) do {                                                 \
    if (cond) { ++pass_count; }                                          \
    else {                                                               \
        ++fail_count;                                                    \
        if (fail_count <= 10u)                                           \
            printf("FAIL %s:%d: %s\n", __FILE__, __LINE__, #cond);       \
    }                                                                    \
} while (0)

static uint64_t xs(uint64_t *s)
{
    uint64_t x = *s; x ^= x << 13; x ^= x >> 7; x ^= x << 17; *s = x;
    return x;
}

/* ------------------------------------------------------------------ */
static void test_init(void)
{
    cos_v96_diffuser_t a, b;
    cos_v96_diffuser_init(&a);
    cos_v96_diffuser_init(&b);
```
