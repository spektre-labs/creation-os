<!-- v111: autogenerated by tools/docs/generate_kernel_pages.py -->
# v99 — σ-Causal

Canonical directory: [`src/v99/`](../../src/v99/).

## Abstract

v99 kernel — see source header for the authoritative description.

## Source

- `src/v99/causal.c`
- `src/v99/causal.h`
- `src/v99/creation_os_v99.c`

## Self-test

```bash
make check-v99
```

## Relation to the σ-stack

Layer membership (cf. `docs/AGI_ARCHITECTURE.md`):

- Layer 4 (Reasoning) — σ-Attractor: stability, convergence, phase-portrait visualisations.

## Source-header excerpt

```c
/* SPDX-License-Identifier: AGPL-3.0-or-later
 *
 * Creation OS v99 — σ-Causal self-test driver.
 */

#include "causal.h"

#include <stdint.h>
#include <stdio.h>
#include <string.h>

static uint32_t pass_count = 0;
static uint32_t fail_count = 0;

#define CHECK(cond) do {                                                 \
    if (cond) { ++pass_count; }                                          \
    else {                                                               \
        ++fail_count;                                                    \
        if (fail_count <= 10u)                                           \
            printf("FAIL %s:%d: %s\n", __FILE__, __LINE__, #cond);       \
    }                                                                    \
} while (0)

static uint64_t xs(uint64_t *s)
{
    uint64_t x = *s; x ^= x << 13; x ^= x >> 7; x ^= x << 17; *s = x;
    return x;
}

static inline int32_t iabs(int32_t x) { return x < 0 ? -x : x; }

/* ------------------------------------------------------------------ */

static void test_init(void)
{
```
