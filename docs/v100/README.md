<!-- v111: autogenerated by tools/docs/generate_kernel_pages.py -->
# v100 — σ-Hyena

Canonical directory: [`src/v100/`](../../src/v100/).

## Abstract

v100 kernel — see source header for the authoritative description.

## Source

- `src/v100/creation_os_v100.c`
- `src/v100/hyena.c`
- `src/v100/hyena.h`

## Self-test

```bash
make check-v100
```

## Relation to the σ-stack

Layer membership (cf. `docs/AGI_ARCHITECTURE.md`):

- Layer 2–3 (Generation + σ-Governance) — σ-Hyena: the 40-kernel closer, σ-governed long-context sequence scan.

## Source-header excerpt

```c
/* SPDX-License-Identifier: AGPL-3.0-or-later
 *
 * Creation OS v100 — σ-Hyena self-test driver.
 */

#include "hyena.h"

#include <stdint.h>
#include <stdio.h>
#include <string.h>

static uint32_t pass_count = 0;
static uint32_t fail_count = 0;

#define CHECK(cond) do {                                                 \
    if (cond) { ++pass_count; }                                          \
    else {                                                               \
        ++fail_count;                                                    \
        if (fail_count <= 10u)                                           \
            printf("FAIL %s:%d: %s\n", __FILE__, __LINE__, #cond);       \
    }                                                                    \
} while (0)

static uint64_t xs(uint64_t *s)
{
    uint64_t x = *s; x ^= x << 13; x ^= x >> 7; x ^= x << 17; *s = x;
    return x;
}

static inline int32_t iabs(int32_t x) { return x < 0 ? -x : x; }

/* ------------------------------------------------------------------ */

static void test_init(void)
{
```
